language = "bash"
run = "killall -q python3 > /dev/null 2>&1; pip install -r requirements.txt && gunicorn --workers=3 app:app"
onBoot = "killall -q python3 > /dev/null 2>&1; pip install -r requirements.txt && gunicorn --workers=3 app:app"